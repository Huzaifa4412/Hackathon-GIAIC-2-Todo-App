# Database Connection (Neon PostgreSQL)
# Get connection string from: https://console.neon.tech
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# Authentication Secrets (CRITICAL: Generate your own secrets!)
# Generate with: openssl rand -base64 32
# BETTER_AUTH_SECRET and JWT_SECRET should be at least 32 characters
BETTER_AUTH_SECRET=your-better-auth-secret-min-32-characters
JWT_SECRET=your-jwt-secret-min-32-characters

# Frontend URL (for OAuth redirect_uri)
# Local development: http://localhost:3000
# Production: https://your-domain.vercel.app
FRONTEND_URL=http://localhost:3000

# Server Configuration
HOST=0.0.0.0
PORT=8000
DEBUG=false

# Google OAuth Credentials (for Google sign-in)
# Get these from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
